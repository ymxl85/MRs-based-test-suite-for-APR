--- a/pt2.c
+++ b/pt2.c
@@ -157,7 +157,7 @@
         if(ch==EOF)unget_error(*tp);
         return(buffer);
       }
-   if(is_spec_symbol(ch1)==TRUE)     /* if end character is special_symbol */
+   if((37 <= ch))     /* if end character is special_symbol */
       { ch=unget_char(ch,tp);        /* then put back this character       */
         if(ch==EOF)unget_error(*tp);
         return(buffer);
