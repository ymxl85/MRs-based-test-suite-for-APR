--- a/pt2.c
+++ b/pt2.c
@@ -196,7 +196,7 @@
     }
 
  if(str_com_id==2)    /* is comment token */
-  { if(ch=='\n' || ch == '\t')     /* for comment until meet end of line */ /*fault*/
+  { if((10 == ch))     /* for comment until meet end of line */ /*fault*/
         return(TRUE);
       else
         return(FALSE);
