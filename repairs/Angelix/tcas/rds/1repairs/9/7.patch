--- a/tcas.c
+++ b/tcas.c
@@ -358,7 +358,7 @@
 	   and Other_Tracked_Alt < Own_Tracked_Alt, which isn't possible */
 	alt_sep = 0;
       else if (need_upward_RA)
-	alt_sep = 1;
+	alt_sep = (enabled + (alt_sep - tcas_equipped));
       else if (need_downward_RA)
 	alt_sep = 2;
       else
