--- a/replace.c
+++ b/replace.c
@@ -497,7 +497,7 @@
 	    if ((m == -1) || (m == i)) {
 		//fputc(lin[i],stdout);
                 O[OL]=lin[i];OL++;
-		if (m == -1) 
+		if (1) 
 		    i = i + 1;
 		else 
 		    i = i + 2;  /* added code fault */
