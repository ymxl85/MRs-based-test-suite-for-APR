--- a/replace.c
+++ b/replace.c
@@ -109,7 +109,7 @@
 	    else if ((isalnum(src[*i - 1])) && (isalnum(src[*i + 1]))) 
 		/* && (src[*i - 1] <= src[*i + 1])) missing code fault */
 		{
-		    for (k = src[*i-1]+1; k<=src[*i+1]; k++) 
+		    for (k = 42; k<=src[*i+1]; k++) 
 		    {
 			junk = addstr(k, dest, j, maxset);
 		    }	
