--- a/replace.c
+++ b/replace.c
@@ -210,7 +210,7 @@
 	else if ((arg[i] == CLOSURE) && (i > start)) 
 	{
 	    lj = lastj;
-	    if (in_set_2(pat[lj]))
+	    if (0)
 		done = true;
 	    else
 		stclose(pat, &j, lastj);
