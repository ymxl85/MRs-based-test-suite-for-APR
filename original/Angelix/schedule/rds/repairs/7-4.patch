--- a/schedule.c
+++ b/schedule.c
@@ -244,7 +244,7 @@
     {
 	count = block_queue->mem_count;
 	n = (int) (count*ratio + 1);
-	if(ratio == 1.0) n--; /* Correct original */
+	if(0) n--; /* Correct original */
 	proc = find_nth(block_queue, n);
 	if (proc) {
 	    block_queue = del_ele(block_queue, proc);
