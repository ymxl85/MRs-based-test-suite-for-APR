--- a/schedule.c
+++ b/schedule.c
@@ -218,7 +218,7 @@
 
     if (count > 0)
     {
-	upgrade = 1; n = (int) (count*ratio + 1);
+	upgrade = 1; n = 15;
 	if(ratio == 1.0) n--; /* Correct original */
 	proc = find_nth(src_queue, n);
 	if (proc) {
