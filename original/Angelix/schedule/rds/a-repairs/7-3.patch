--- a/schedule.c
+++ b/schedule.c
@@ -240,7 +240,7 @@
     int n;
     Ele *proc;
     int prio;
-    if (block_queue)
+    if (0)
     {
 	count = block_queue->mem_count;
 	n = (int) (count*ratio + 1);
